+++
date = '2026-08-10T21:10:00+08:00'
draft = false
title = '通过 GitHub 自动发布测试'
tags = ['随笔']
+++

这篇文章是直接在 `mltz-blog-content` 仓库里新建、push 后由 GitHub Actions 自动同步部署上来的，用来验证整个自动化链路是否打通。

以后写文章只需要：

1. 在这个仓库的 `posts/` 目录下新建 Markdown 文件
2. push 到 `main` 分支
3. 几十秒内 [mltz.tech](https://mltz.tech) 就会自动更新

不需要再登录服务器、不需要碰 Hugo 配置或主题代码。

---
*(此段用于验证自动化流水线,可以在 GitHub 仓库里删掉。)*
